package com.dongkeun.camerawithvision.etc.post

import androidx.lifecycle.ViewModel

class PostViewModel : ViewModel() {
    /** TODO
     * 1. 내가 쓴 포스트가 존재하는지 조회 (있으면 adapter로 출력, 없으면 없다고 텍스트 띄우기)
     * 2. 각각을 쓴 날짜, 좋아요, 덧글 조회 (조회수는 포함시키지 말자)
     */
}